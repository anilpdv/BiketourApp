# BikeTourEurope Environment Configuration
# No API keys required - all services are free!

# The app uses:
# - Wikimedia Commons for POI photos (free, no key)
# - OpenStreetMap/Nominatim for search (free, no key)
# - OSRM for routing (free, no key)
# - OpenFreeMap for map tiles (free, no key)
# - Open-Meteo for weather (free, no key)
